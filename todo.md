# FlowCut TODO - 자막 시스템 & 내보내기 안정화

## 🔴 긴급 (현재 세션에서 해결 필요)

### 1. 자막 한 줄 제한 적용
- [ ] `server/server.cjs` chunking 코드에서 `maxLines = 2` → `maxLines = 1`
- [ ] `\N` (ASS 하드 줄바꿈) 삽입 로직 제거
- [ ] 15자 초과 문장 → 시간 기반 청크 분할 (한 번에 한 줄만 표시)
- [ ] 청크 간 fade-in/out으로 자연스러운 전환

### 2. 카라오케 단어 간 공백 추가
- [ ] `karaokeText` 생성 시 `word.text` 뒤에 공백 삽입 (마지막 단어 제외)
- [ ] emphasis 모드에서도 동일 적용

### 3. Export `-map` 인자 확인/복원
- [ ] `server/server.cjs`에서 `-map [lastVideo]`, `-map [audioLabel]` push 확인
- [ ] 없으면 d6b6362 커밋의 원본 로직에서 복원

### 4. 새 스크립트로 전체 파이프라인 테스트
- [ ] 기존 media_cache 정리 또는 새 스크립트 실행
- [ ] TTS → VideoExtend → BGM → 자막 → Export 전체 흐름 검증
- [ ] 자막-내레이션 타이밍 일치 확인

---

## 🟡 중기 (안정화 후)

### 5. 프리뷰 자막 오버레이
- [ ] 프론트엔드에 Canvas/HTML 기반 자막 렌더러 추가
- [ ] ASS 파일 파싱 → 실시간 타이밍에 맞춰 자막 표시
- [ ] 프리셋 스타일을 프리뷰에도 반영 (색상, 폰트, 애니메이션)
- [ ] 편집 중 자막 on/off 토글

### 6. 자막 프리셋 UI (ExportPanel 또는 별도 패널)
- [ ] 8개 프리셋 선택 드롭다운/카드 UI
- [ ] 프리셋별 미리보기 썸네일
- [ ] 커스텀 스타일 오버라이드 폼 (폰트, 색상, 크기, 위치 등)
- [ ] 선택한 프리셋을 export API에 `style.preset` 으로 전달

### 7. ScriptEngine 자연어 → 프리셋 자동 선택
- [ ] `STYLE_KEYWORDS` 활용하여 스크립트 프롬프트에서 자막 스타일 자동 매칭
- [ ] 예: "웹툰 스타일로" → webtoon, "영화 자막" → cinematic
- [ ] ScriptEngine에서 `/api/subtitle/generate` 호출 시 preset 파라미터 자동 설정

### 8. 선택적 효과 적용 (클립/문장 단위)
- [ ] 특정 클립이나 문장에만 다른 프리셋 적용 가능하도록 segments에 `styleOverride` 필드 추가
- [ ] UI에서 타임라인의 각 자막 세그먼트를 클릭하여 개별 스타일 변경
- [ ] 스크립트 자연어 예: "첫 번째 장면은 임팩트, 나머지는 시네마틱"

---

## 🟢 장기 (고급 기능)

### 9. 고급 ASS 효과 확장
- [ ] `\move` (위치 이동 애니메이션)
- [ ] `\t` (시간 기반 변환: 색상/크기/투명도 그라데이션)
- [ ] `\clip` / `\iclip` (마스크/크롭 효과)
- [ ] `\org` + `\frz` (회전 효과)
- [ ] 사용자 정의 drawing mode (`\p1`) 도형 배경

### 10. 다국어 자막 지원
- [ ] 한국어 외 영어, 일본어, 중국어 폰트 번들링
- [ ] 언어별 `maxCharsPerLine` 자동 조정 (CJK: 15, Latin: 40)
- [ ] 다국어 동시 표시 (이중 자막)

### 11. 자막 편집기
- [ ] 타임라인 위에 자막 트랙 시각화
- [ ] 드래그로 자막 시작/끝 시간 조정
- [ ] 인라인 텍스트 편집
- [ ] 실시간 ASS 파일 재생성 및 프리뷰 반영

### 12. 자막 템플릿 저장/불러오기
- [ ] 사용자 커스텀 프리셋 저장 (JSON)
- [ ] 프로젝트 간 스타일 공유
- [ ] 커뮤니티 프리셋 다운로드 (향후)

---

## 📁 핵심 파일 참조

| 파일 | 역할 |
|------|------|
| `server/server.cjs` | Export 서버, 자막 생성 API, FFmpeg 호출 |
| `src/config/subtitleEffects.ts` | 프리셋 정의, TypeScript 인터페이스 |
| `src/scripting/ScriptEngine.ts` | 자연어 스크립트 → 자막 세그먼트 수집 |
| `src/components/export/ExportPanel.tsx` | Export UI, API 호출 |

## 🔖 Git 참조 커밋

| 커밋 | 설명 |
|------|------|
| `d6b6362` | 안정적인 export 로직 (자막 추가 전) |
| `b46fde6` | 자막 시스템 최초 도입 (ASS karaoke) |

## ⚠️ 교훈 / 주의사항

1. **PowerShell 문자열 치환의 위험성**: 들여쓰기, 줄바꿈, 이스케이프 차이로 패턴 매칭 실패 → 라인 인덱스 기반 편집이 더 안전
2. **FFmpeg 필터 그래프 경로**: Windows 드라이브 문자(`:`) 반드시 `\:` 이스케이프, 또는 상대 경로 + `cwd` 사용
3. **`const` 재할당 주의**: 배열 필터링 결과를 재할당할 변수는 `let`으로 선언
4. **손상 파일 검증 필수**: VideoExtend 등 외부 프로세스 출력은 항상 파일 크기/유효성 검증
5. **Git 커밋 자주 하기**: 안정 상태마다 커밋하여 롤백 포인트 확보
